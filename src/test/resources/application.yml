spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: faridtest
test:
  topic: "test_topic"

monitor:
  kafka:
    bootstrap:
      config: "PLAINTEXT://localhost:9085"
    consumer:
      groupid: "faridtestGrp"
